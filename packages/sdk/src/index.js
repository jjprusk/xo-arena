// @callidity/sdk — runtime stub
// All exports are TypeScript types only; no runtime values are defined here.
// The platform SDK provider creates the concrete sdk object at runtime and
// passes it into each game component via props.
